import ClipLoader from 'react-spinners/ClipLoader';

const color = '#000';

const LoadingSpinner = () => {
  return (
    <ClipLoader color={color} size={26} />
  );
};

export default LoadingSpinner;
